% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vina.R
\name{run_vina}
\alias{run_vina}
\title{Run molecular docking with AutoDock Vina}
\usage{
run_vina(
  receptor = NULL,
  ligand = NULL,
  center = NULL,
  size = NULL,
  config_paths = NULL,
  exhaustiveness = 8,
  out = "docked",
  logs = "logs",
  vina_path = NULL,
  seed = 12345,
  cpu = NULL
)
}
\arguments{
\item{receptor}{Path to receptor PDBQT file (only for direct mode)}

\item{ligand}{Path to ligand PDBQT file (only for direct mode)}

\item{center}{Numeric vector of length 3 for box center (x,y,z) (only for direct mode)}

\item{size}{Numeric vector of length 3 for box size (x,y,z) (only for direct mode)}

\item{config_paths}{Character vector of paths to config files or directories containing config files}

\item{exhaustiveness}{Integer, search intensity, default 8 (only for direct mode)}

\item{out}{Character, output directory for docked files, default "docked"}

\item{logs}{Character, output directory for log files, default "logs"}

\item{vina_path}{Character, path to vina executable, default NULL}

\item{seed}{Integer, random seed for reproducibility, default 12345}

\item{cpu}{Integer, number of CPU cores to use for parallel computation, default NULL (auto-detect)}
}
\description{
This function runs molecular docking using AutoDock Vina.
}
\examples{
\dontrun{
# Run molecular docking using AutoDock Vina
run_vina(
  config_paths = "configs",
  out = "docked",
  logs = "logs"
)
}
}
